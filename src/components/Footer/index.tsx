import React from 'react';
import {View, Text} from 'react-native';

export default class Footer extends React.Component {
  render() : React.ReactNode {
    return(
      <View>
        <Text>Cálculo de IR para 2020</Text>
      </View>
    );
  }
}